import AuthorInfo from './AuthorInfo';

const AboutSection: React.FC = () => {
  return (
    <section id="about">
      <div>
        <AuthorInfo />
      </div>
    </section>
  );
};

export default AboutSection;
